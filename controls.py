#This should record audio and output fft within a range with a cutoff.
def input(milliseconds, lowFreq, highFreq, decibelCutoff):
	print("Starting to record")
	return None

if __name__ == "__main__":
	input(1000, 20, 800, 1)
